# Artifacts

Proof transcripts live in `runs/$RUN_ID/` (gitignored). Cleanup of `/tmp/phlo-verify-*` must not delete this tree.

Committed example: [project-init.example.md](project-init.example.md), refreshed by `scripts/prove-project-init.sh`.
