[build-system]
requires = ["setuptools>=45", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "phlo-metrics"
version = "0.1.0"
description = "Metrics collection and CLI for Phlo"
readme = {text = "Metrics collection and CLI for Phlo.", content-type = "text/plain"}
requires-python = ">=3.11"
authors = [
    {name = "Phlo Team", email = "team@phlo.dev"},
]
license = {text = "MIT"}
dependencies = [
    "phlo>=0.1.0",
    "cachetools>=6.2.1",
    "psycopg2-binary>=2.9.11",
    "requests>=2.32.5",
]

[project.optional-dependencies]
nessie = [
    "phlo-nessie>=0.1.0",
]
postgres = [
    "phlo-postgres>=0.1.0",
]
dev = [
    "pytest>=7.0",
    "ruff>=0.1.0",
]

[project.entry-points."phlo.plugins.cli"]
metrics = "phlo_metrics.cli_plugin:MetricsCliPlugin"

[project.entry-points."phlo.plugins.hooks"]
metrics = "phlo_metrics.hooks_plugin:MetricsHookPlugin"

[project.entry-points."phlo.plugins.resources"]
metrics = "phlo_metrics.resource_provider:MetricsResourceProvider"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]

[tool.ruff]
line-length = 100
target-version = "py311"
