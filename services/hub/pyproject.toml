[project]
name = "cascade-hub"
version = "0.1.0"
description = "Service hub for Cascade data platform"
requires-python = ">=3.11"
dependencies = [
    "flask>=3.0.0",
]

[tool.ty]
environment.python-version = "3.11"
