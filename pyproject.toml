[project]
name = "lakehouse-hub"
version = "0.1.0"
description = "Service hub for data lakehouse platform"
requires-python = ">=3.11"
dependencies = [
    "flask>=3.0.0",
]

[tool.ty]
environment.python-version = "3.11"
